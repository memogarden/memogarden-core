# MemoGarden Core - Environment Configuration

# Database configuration
DATABASE_PATH=./data/memogarden.db

# API configuration
API_V1_PREFIX=/api/v1

# CORS origins (JSON array format)
# For Tailscale-only deployment, ["*"] is acceptable
# For public deployment, restrict to specific origins
CORS_ORIGINS=["*"]

# Default currency
DEFAULT_CURRENCY=SGD

# Authentication configuration
# IMPORTANT: Change this in production! The install script generates a secure key.
JWT_SECRET_KEY=change-me-in-production-use-env-var
JWT_EXPIRY_DAYS=30

# Testing configuration (set to "true" to bypass localhost checks)
# DO NOT set to true in production
BYPASS_LOCALHOST_CHECK=false
